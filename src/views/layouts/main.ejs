<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Desafio 29</title>

    <link
      rel="stylesheet"
      href="https://bootswatch.com/5/quartz/bootstrap.min.css"
    />
  </head>
  <body>
    <nav class="navbar navbar-expand-lg bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="/">Autenticacion</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav text-left">
            <% if (user) { %>
            <li class="nav-item text-end">
              <a class="nav-link hover:text-dark" href="/profile">Perfil</a>
            </li>
            <li class="nav-item text-end">
              <a class="nav-link" href="/logout">Cerrar Sesion</a>
            </li>
            <% } else {%>
            <li class="nav-item text-end">
              <a class="nav-link hover:text-dark" href="/login"
                >Iniciar Sesion</a
              >
            </li>
            <li class="nav-item text-end">
              <a class="nav-link" href="/register">Registrarse</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <% if (signupMessage.length > 0) { %>
      <div class="alert alert-danger mt-2"><%= signupMessage %></div>
      <% } %> <% if (signinMessage.length > 0) { %>
      <div class="alert alert-danger mt-2"><%= signinMessage %></div>
      <% } %> <%- body %>
    </div>

    <script src="/socket.io/socket.io.js"></script>
    <script src="/clientSocket/socket.js"></script>
  </body>
</html>
